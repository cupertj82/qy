name: worker

on:
  push:

jobs:
  breed:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        breed: ["glitter", "threaten", "depart", "reflection", "temporary"]
        specimen: ["instance", "quiet", "creep", "employ"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar OF
